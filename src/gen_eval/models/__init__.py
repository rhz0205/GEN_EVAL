"""Lightweight model adapters used by GEN_EVAL metrics.

This package is for small project-local adapter code only. Model weights and
checkpoints must stay outside source code, typically under ``pretrained_models/``.
"""
