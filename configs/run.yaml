run:
  dataset_name: geely
  data_count: 10
  timestamp: "20260429022524"
  profile: debug

  paths:
    data_file: data/geely_10_20260429022524.json
    output_dir: outputs/geely/10_20260429022524

  stages:
    inspect_data: true
    prepare_reference: true
    evaluate: true
    summarize: true
    visualize: false

  runtime:
    device: cuda

  profiles:
    debug:
      runtime:
        backend: local_multi_gpu
        device: cuda
        devices:
          - 0
    eval:
      runtime:
        backend: ray
        device: cuda
        ray_address: auto
        shard_size: 1
        num_cpus_per_task: 4
        num_gpus_per_task: 1

  logging:
    level: INFO
